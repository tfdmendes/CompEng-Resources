#include <stdio.h>
#include <math.h>

int main(){

    scanf()

    for(int i = 1; i < 10; i++){
        double power, squareRoot;
        power = pow(i, 2);
        squareRoot = sqrt(i);
        printf("Quadrado de %d: %10.0f \n", i, power);
        printf("Raiz quadrada de %d: %10.2f \n\n", i, squareRoot);
    }

    return 0;
}